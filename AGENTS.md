# 深度学习科研工作规则

- 默认用中文回复，代码、命令和报错保持原文。
- 先给结论和最小必要步骤，不重复粘贴完整训练日志。
- 检查代码时优先读取相关文件和关键片段，不扫描或输出整个数据集、模型权重、checkpoint、runs 和日志目录。
- 运行训练、评估或长任务前，先确认命令、GPU、显存和输出目录；长任务放后台运行，只汇报关键日志和异常。
- 终端输出优先使用摘要：错误保留关键上下文，训练日志保留最近阶段、loss、主要指标和最佳结果。
- 每完成一个阶段（数据处理、训练、评估、论文图表）总结：已完成、关键结果、下一步。
- 论文最终实验矩阵固定为四张表（C3 对比、C3 消融、C4 对比、C4 消融）：每张表左侧第一列必须是模型/消融方法，右侧第一层表头按数据集分组，第二层表头列出该数据集指标（如 AP、AUROC、P、R、F1 或 BMS 误报指标）。已完成实验单元直接填实际指标数值，并在数值后注明任务号；未完成填“待补”，旧协议数值加 `†`，条件补跑外部单元加 `*`。不得把表格改成“数据集为行”的进度表来替代该版式。
- 更新最终实验矩阵时只允许替换已完成实验单元的指标值、任务号和必要的协议标记；不得增删行列、改动表头层级、调整四表版式或把数值表改成其他结构。后台尚未完成的结果不得提前写入最终矩阵。
