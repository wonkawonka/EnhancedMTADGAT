# Legacy CUDA 11.8 environment for DGL/PyG external baselines only.
# Do not install this file for the main model on a current Kaggle image; it pins
# and replaces PyTorch. Use requirements-kaggle-main.txt for the main project.
#   pip install -r requirements-kaggle-cu118.txt

--extra-index-url https://download.pytorch.org/whl/cu118
--find-links https://data.dgl.ai/wheels/torch-2.2/cu118/repo.html
--find-links https://data.pyg.org/whl/torch-2.2.0+cu118.html

-r requirements.txt

# Keep the CUDA graph stack on one compatible torch family.
numpy==1.26.0
torch==2.2.0+cu118
torchvision==0.17.0+cu118
torchaudio==2.2.0+cu118
torchdata==0.7.1

# DGL and PyG wheels must match torch 2.2 + cu118.
dgl==2.2.1
pyg-lib
torch-scatter
torch-sparse
torch-cluster
torch-spline-conv
torch-geometric>=2.5,<2.8

# External baseline metrics and time-series feature extraction.
pytz
Pillow>=9.4,<12
statsmodels>=0.14,<1
tsfresh>=0.20,<1
hurst>=0.0.5
arch>=6.1,<9

# The current external plan does not run OmniAnomaly, so TensorFlow/TFSnippet
# are deliberately excluded from this Kaggle environment.
