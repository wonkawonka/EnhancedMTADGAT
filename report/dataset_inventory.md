# 数据集与预处理台账

本文档记录正式实验实际使用的数据、变量职责、划分和评价边界。所有窗口都按实体、片段、电池或电池簇独立构造，不跨边界拼接；缩放器只拟合训练数据。

## 1. 数据集角色

| 数据集 | 本地状态 | 标签 | 用途 | 主表 |
| --- | --- | --- | --- | --- |
| NC Battery（清华团队） | 三品牌已下载并通过官方 MD5 校验 | 车辆级正常/故障 | 第三、四章电池主数据 | 是 |
| MSL | 已预处理 | 时间点异常区间 | 第三章通用基准 | 是 |
| NASA Random Walk Discharge | 已预处理 | 充/放/静置工步，非故障 | 状态表征探针 | 否 |
| 自有 BMS | 原始Excel需预处理 | 测试段确认正常，无故障标签 | 调频工况误报与条件化稳定性 | 否 |
| CH-BATTERY | 兼容/备用 | 样本级标签 | 当前不进入正式计划 | 否 |

窗口数公式：长度 (N>L) 的独立序列在步长 (s) 下产生

```text
1 + floor((N - L - 1) / s)
```

个“历史窗口—下一时刻”样本。

## 2. NC Battery（Realistic fault detection）

- 官方 Figshare 数据为三个品牌包，总计 347 辆车（292 正常、55 故障）、700,582 个充电片段，压缩后约 1.38 GB。
- 三品牌分别训练，不混合。实际片段覆盖 brand1/2/3 的 198/49/100 辆车，合计 347 辆；故障车分别为 30/16/9 辆。brand2 标签表另有 1 辆车没有任何片段，正式评价不计入。
- 每个片段包含 `pack_voltage, current, soc, max_cell_voltage, min_cell_voltage, max_temperature, min_temperature`；原始时间列不作为模型特征。
- 条件通道为 `current, soc`；报警响应为总电压、单体电压极值和温度极值。
- 标签是车辆级。故障车的每个片段不都视为异常样本。

每个品牌独立做车辆级五折。正常车辆的一折测试、一折验证、其余训练；故障车辆只进入测试。内部模型的缩放器仅拟合当折正常训练车辆并逐通道 MinMax。官方 DyAD 代码不是 MinMax，而是按均值及 `max(std, 0.1×range)` 缩放，且实现中只抽取前 200 个训练片段估计统计量；复现 DyAD 时保留其原始设置，本文内部模型则统一使用完整正常训练集统计量。每辆车取最高 5% 片段分数的均值。主报车辆级 AUROC、AUPRC 和低误报率下 TPR。

### 归一化口径

归一化并非每个数据集随意选择，而是按变量语义分两类：

| 数据 | 方法 | 原因 |
| --- | --- | --- |
| MSL | 训练集 MinMax | 匿名遥测，无可靠物理零点，保持通用基线口径 |
| NC Battery | 逐通道 MinMax | 与 MTAD-GAT 常规预处理一致；仅用正常训练车辆拟合，避免人为分组尺度失真 |
| NASA Random | 电压/温度 Robust，电流绝对值 P95 | 保留充放电正负方向，避免工步切换被 MinMax 改写 |
| BMS | 分组 MaxAbs | 35 维含有符号量、差值和比值，统一保留物理零点 |

不同缩放器只改变数值尺度，不使用测试统计量，也不作为模型创新。真正必须统一的是：只拟合训练数据、电流零点不漂移、同类物理量保持可比较。

## 3. MSL

- 27 个航天器实体，55 个匿名数值通道。
- 目标与报警：`channel_0`。
- 潜在上下文：`channel_1`–`channel_54`；没有物理语义，不称为电池工况，并关闭电流/SOC辅助任务。
- 官方训练序列按实体和时间取前 90% 训练、后 10% 正常校准；官方测试集完整保留。

| 子集 | 序列 | 时间点 | `lookback=100, stride=1` 窗口 | 异常点 |
| --- | ---: | ---: | ---: | ---: |
| 训练 | 27 | 52,473 | 49,773 | 0 |
| 正常校准 | 27 | 5,844 | 3,255 | 0 |
| 测试 | 27 | 73,729 | 71,029 | 7,766 |

MinMaxScaler 只拟合训练段。报告 raw AUPRC、point-F1、precision、recall、AUROC 和 event 指标；point-adjusted 仅作附录。

## 4. NASA Random Walk Discharge

- 电池：RW1、RW2、RW7、RW8。
- 原始处理文件保存 `step_type_code, voltage, current, temperature`，但加载时删除 `step_type_code`。
- 模型输入为 `voltage, current, temperature`；状态编码器只读 `current`，响应评分只读 `voltage, temperature`。
- 事后探针按电流方向形成静置/充电/放电组，只是弱表征检查，不是故障标签或模型输入。
- 电流方向分组只用于冻结编码器后的线性探针，不参与异常模型训练。

正式验证用 RW1/RW2/RW7 训练，RW8 测试；按工步边界保存独立序列，原始点步长 4 下采样。电压/温度按训练集 IQR 缩放，电流以 0 为中心并按训练集绝对值 P95 缩放。

| 数据 | 段数 | 时间点 | `lookback=100, stride=4` 窗口 |
| --- | ---: | ---: | ---: |
| RW1/RW2/RW7 训练合计 | 51 | 1,415,256 | 352,556 |
| RW8 测试 | 8 | 111,640 | 27,714 |

仅报告电流方向探针 macro-F1/accuracy 和跨电池稳定性，不写成工步识别能力或故障检测 F1。

## 5. 自有 BMS

- 2024-07-01 至 2024-07-31；因关键表缺失跳过 7 月 20、23 日，使用 29 天。
- 6 个电池簇，每簇 250,290 点；按时间 80%/20% 划分。
- 原始系统、簇、单体电压和单体温度表对齐后形成 35 个特征。

### 5.1 变量职责

- 条件输入：系统总电流 `SYS_I`、簇 SOC `BMSnRSOC`。
- 必须参与报警：簇电流 `BMSnI`。
- 其他报警响应：簇端电压，簇电压/温度极值和均值，单体电压/温度标准差、极差、偏离量，以及簇—系统层级差异。
- 不直接聚合到报警：系统级运行量、SOC/SOH和允许电流上限等条件/约束量。

这使模型可以学习“给定系统调频电流和 SOC，某簇应承担怎样的电流及电—热响应”。某个并联簇电流份额异常时可能升高分数。串联单体没有独立电流观测，只能通过电压、温度离散度间接发现不一致。

### 5.2 预处理与规模

表格按时间对齐、去重；缺失值依次线性插值、前后填充、最后填 0。派生单体离散度和簇—系统差异。MaxAbsScaler 只拟合训练段并保持电流零点；同类电压和温度共享尺度。

| 子集 | 簇数 | 每簇点数 | `lookback=100, stride=4` 总窗口 |
| --- | ---: | ---: | ---: |
| 训练 | 6 | 200,232 | 300,198 |
| 测试 | 6 | 50,058 | 74,940 |

告警表目前不能形成可靠故障标签，且数据只有调频运行，缺少调峰、静置或其他上层任务工况的同源对照，因此不能做调峰—调频或静置—调频泛化实验。现有 BMS 只验证单一调频任务内部的运行状态变化：六个簇、电流强度/方向、SOC和时间块。在确认测试段全程正常的前提下，超过正常训练阈值的窗口按经验误报统计；比较总体误报率、每万窗口误报数、最坏组超额FPR、跨簇/电流/SOC/时间块误报波动和阈值归一化评分稳定性。这里仍不报告故障 F1、召回率、过流检出能力或跨任务工况泛化；要证明这些能力，还需调峰等其他任务数据、工程阈值事件、人工确认事件或受控注入实验。

## 6. 当前不进入主线的数据

- CH-BATTERY LFP：兼容加载器保留 7 个汇总字段，但本地有 85 个 NUL 伪 CSV；修复文件并增加独立正常验证集前不恢复正式计划。
- SMD、NASA Random Charge、CALCE/CALCE2：代码仍兼容，但当前原始数据缺失或不属于论文计划。
- 经典 NASA 和 SMAP 已从命令入口、训练/预测与报告中移除；磁盘中的原始文件不由代码清理操作自动删除。
